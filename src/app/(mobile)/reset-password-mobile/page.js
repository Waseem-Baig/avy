import React from "react";
import Layout from "../layout";
import ResetPasswordM from "./resetPasswordMobile";

const ResetPasswordMobile = () => {
  return (
    <Layout>
      <ResetPasswordM />
    </Layout>
  );
};

export default ResetPasswordMobile;
